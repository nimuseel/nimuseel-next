import styled from '@emotion/styled';
import { CompanyAndProjectPeriod } from '../styles';

const CompanyName = styled.h2`
  margin: 0;
  ${CompanyAndProjectPeriod}
`;

const Position = styled.p`
  margin-bottom: 8px;
`;

export default {
  CompanyName,
  Position,
};
